name: gensim
version: 0.0.6
homepage: https://github.com/relf108/gensim
description: Gensim is an API which allows developers to quickly and easily set up simulations of the evolutionary process.
environment:
  sdk: ">=2.9.3 <3.0.0"
dependencies:
  meta: ^1.2.3
dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
