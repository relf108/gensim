///A skill can be given to an entity so that the entity can use that skill
class Skill {
  ///would like a trait to take a method

  var name;
  var function;

  Skill({String name, Function function}) {
    this.name = name;
    this.function = function;
  }
}
